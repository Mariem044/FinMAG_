# Voici un plan de correction priorisé, du plus critique au cosmétique.

# 🔴 PRIORITÉ 1 — ETL / Données (bloquant)
# 1. FAIT_ECRITURES — données fantômes

# EC_Date remplacée par date_extraction → corriger le mapping dans pipeline.py _assemble_fait_ecritures
# EC_Montant à 0 partout → vérifier que df1 (écriturec) et df2 (regtaxe) ne sont pas vides après extraction
# EC_No tronqué en "EC-" → le champ n'est pas résolu, vérifier la colonne dans extract_fait_ecriturec

# 2. Familles produits — hash codes exposés

# "Famille 91102577" dans tous les modules → FA_CodeFamille n'est pas joint à son libellé
# Ajouter FA_Intitule dans DIM_FAMILLE et le propager via DIM_ARTICLE

# 3. Stock à 0 dans Inventory

# AS_MontSto, AS_QteSto tous à zéro → extract_fait_artstock() retourne probablement vide
# Vérifier que F_ARTSTOCK est accessible sur MAG_ENGINE et que le pipeline charge bien df4 dans _assemble_fait_ecritures

# 4. DSI et RFM non calculés

# _compute_dsi_jours() et _compute_rfm_scores() dépendent de FAIT_ECRITURES et FAIT_LIGNES_VENTE corrects → se débloquent automatiquement après fix #1 et #3


# 🟠 PRIORITÉ 2 — Calculs incohérents (trompeur)
# 5. -28.7% croissance vs +8.2% vs N-1 sur le même CA

# Deux métriques calculées différemment dans queries.py → harmoniser la formule dans get_dashboard_kpis() et get_ca_by_month()

# 6. Taux rapprochement 0% avec +1.8% vs N-1

# RT_Rapproche toujours à 0 → BR_Rapproch non résolu dans _assemble_fait_reglements, vérifier le .fillna(0)

# 7. Pourcentages KPI-24 à 67 millions %

# Division par quasi-zéro dans le calcul des parts de mouvements caisse → ajouter un guard if total > 0 côté frontend ou dans get_caisse_mouvements_by_type()

# 8. DIM_TYPE_MVT_CAISSE non résolu

# "Mouvement 8", "Mouvement 54" → _add_static_label() dans pipeline.py génère "Mouvement {int(v)}" mais sans libellé métier. Alimenter une vraie table de correspondance ou récupérer les libellés depuis la source


# 🟡 PRIORITÉ 3 — UI / Affichage (dégradant)
# 9. CA tronqué partout — "16 503 9..."

# Fix CSS sur les KPI cards : font-size adaptatif ou text-overflow géré, ou formatter les grands nombres en "16.5 MDT" systématiquement

# 10. Légende RFM tronquée — "Fonnance", "Eartonn"

# Overflow CSS sur la légende du scatter plot → white-space: nowrap + min-width sur les items de légende

# 11. Chart Aging KPI-08 vide sans message

# Ajouter un état vide explicite : "Aucune créance en souffrance détectée" avec icône verte si données = 0

# 12. KPI-13 DSI vs CA vide sans message

# Même pattern → état vide conditionnel à afficher

# 13. Axe X scatter RFM sans label

# Ajouter le label "Fréquence (commandes)" sur l'axe X

# 14. Pie chart Dashboard — DISTRIBUTEUR 100%

# Masquer le chart dynamiquement si un seul segment représente 100% et afficher "Données insuffisantes pour la répartition"

# 15. Sidebar active state incorrect

# Sur Inventory, "Revenue & Sales" restait surligné → vérifier la logique de route active dans le composant Nav


# 🔵 PRIORITÉ 4 — UX / Expérience (polish)
# 16. Courbes qui chutent à 0 en mars

# Ajouter une annotation visuelle "Mois en cours (données partielles)" sur le dernier point des line charts

# 17. Solde chèques -15K DT sans alerte

# Badge rouge automatique si solde < 0 sur les cards caisses

# 18. Prévision -186K DT sans alerte sur le KPI card

# Colorer en rouge le KPI "Prévision solde J+15" si valeur négative

# 19. Taux rapprochement 100% badge rouge

# Inverser la logique de couleur : 100% doit être vert, pas rouge avec "-2.1%"

# 20. Float 20j identique sur tous les bordereaux

# Vérifier que LB_NbJour est bien varié dans les données source, sinon afficher "N/D" plutôt qu'une fausse précision


